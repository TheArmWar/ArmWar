#pragma once

/**
 * This file is an interface for each user to fill the credentials used by the
 * controller to connect to a network
 */
#define SECRET_SSID "your_ssid"
#define SECRET_PASSWORD "your_password"
